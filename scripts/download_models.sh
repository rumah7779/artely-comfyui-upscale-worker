#!/usr/bin/env bash
set -euo pipefail

echo "Model downloader placeholder."
echo "Recommended production setup: mount a RunPod network volume with models already cached."
echo "If you want startup downloads, add safe model URLs to models.json and extend this script."

